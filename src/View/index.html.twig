{% extends "base.html.twig" %}

{% block content %}
    <ul>
        {% for link in links %}
            <li>
                <a href="{{ link.href }}">
                    {{ link.title }}
                </a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}